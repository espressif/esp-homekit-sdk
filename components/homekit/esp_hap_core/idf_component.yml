## IDF Component Manager Manifest File
dependencies:
  espressif/libsodium:
    version: "~1.0.20"
    rules:
      - if: "idf_version >=5.0"
  espressif/mdns:
    rules:
      - if: "idf_version >=5.0"
    
